<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="data"
            type="com.doctor.sun.entity.Prescription" />

        <variable
            name="adapter"
            type="com.doctor.sun.ui.adapter.core.SortedListAdapter" />

    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="@dimen/dp_4"
        android:paddingLeft="@dimen/dp_13"
        android:paddingRight="@dimen/dp_13"
        android:visibility="@{data.enabled}">

        <TextView
            android:id="@+id/et_others"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/ripple_prescription"
            android:gravity="start|center_vertical"
            android:onClick="@{()->data.modify(context)}"
            android:paddingBottom="@dimen/dp_8"
            android:paddingLeft="@dimen/dp_13"
            android:paddingRight="@dimen/dp_4"
            android:paddingTop="@dimen/dp_8"
            android:text="@{data.label}"
            android:textColor="@color/color_tick"
            android:textSize="@dimen/font_15" />

        <FrameLayout
            android:id="@+id/tv_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:layout_marginEnd="@dimen/dp_35"
            android:layout_marginRight="@dimen/dp_35"
            android:background="@drawable/ic_delete_prescription"
            android:onClick="@{()->data.removeThis(adapter)}">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/ripple_default"
                android:gravity="center"
                android:textColor="#fff"
                android:textSize="@dimen/font_17" />
        </FrameLayout>
    </LinearLayout>
</layout>